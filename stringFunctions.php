<?php

$array = array("im", "jh", "iro", "zen");
$username = "im jh iro zen";
$phoneNum = "123-456-7986";

// $username = strtolower($username);                           // Make string lowecase
// $username = strtoupper($username);                           // Make string uppercase
// $username = trim($username);                                 // remove extra space first and the last
// $username = str_pad($username, 20, "+");                     // add char depend length of the string
// $phoneNum = str_replace("-", "/", $phoneNum);                // replace delimiter to the new one
// $username = strrev($username);                               // reverse the string
// $username = str_shuffle($username);                          // shuffle the string
// $equal = strcmp($username, "imjhirozen");                    // compare the string it return 0 if true else -1
// $count = strlen($username);                                  // return the length of the string
// $count = strpos($username, " ");                             // find the specific target on the string  
// $username = substr($username, 0, strlen($username) -6);      // cut the string depend what length you want
//$fullname = explode(" ", $username);                          // convert string to an array but it need a delimiter
//$fullname = implode("/", $array);                             // convert array to a string and a delimiter

echo $fullname;

/* foreach($fullname as $name){
    echo $name . "<br>";
} */